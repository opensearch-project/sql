calcite:
  logical: |
    LogicalSystemLimit(fetch=[10000], type=[QUERY_SIZE_LIMIT])
      LogicalProject(yyyy-MM-dd=[$0])
        LogicalFilter(condition=[<($0, DATE('2018-11-09 00:00:00.000000000'))])
          LogicalProject(yyyy-MM-dd=[$0])
            LogicalFilter(condition=[>($0, DATE('2016-12-08 00:00:00.123456789'))])
              LogicalProject(yyyy-MM-dd=[$83])
                CalciteLogicalIndexScan(table=[[OpenSearch, opensearch-sql_test_index_date_formats]])
  physical: |
    EnumerableLimit(fetch=[10000])
      EnumerableCalc(expr#0..94=[{inputs}], expr#95=[Sarg[('2016-12-08':EXPR_DATE VARCHAR..'2018-11-09':EXPR_DATE VARCHAR)]:EXPR_DATE VARCHAR], expr#96=[SEARCH($t83, $t95)], yyyy-MM-dd=[$t83], $condition=[$t96])
        CalciteEnumerableIndexScan(table=[[OpenSearch, opensearch-sql_test_index_date_formats]])
