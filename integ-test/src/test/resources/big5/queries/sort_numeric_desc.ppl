/*
{
  "name": "sort_numeric_desc",
  "operation-type": "search",
  "index": "{{index_name | default('big5')}}",
  "body": {
    "track_total_hits": false,
    "query": {
      "match_all": {}
    },
    "sort": [
      {
        "metrics.size": "desc"
      }
    ]
  }
}
*/
source = big5
| sort - `metrics.size`
| head 10