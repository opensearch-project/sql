calcite:
  logical: |
    LogicalSystemLimit(fetch=[10000], type=[QUERY_SIZE_LIMIT])
      LogicalProject(host=[$FIELD_INDEX])
        LogicalFilter(condition=[CIDRMATCH($FIELD_INDEX, '0.0.0.0/24':VARCHAR)])
          CalciteLogicalIndexScan(table=[[OpenSearch, opensearch-sql_test_index_weblogs]])
  physical: |
    EnumerableLimit(fetch=[10000])
      EnumerableCalc(expr#0..11=[{inputs}], expr#12=['0.0.0.0/24':VARCHAR], expr#13=[CIDRMATCH($FIELD_INDEX, $t12)], host=[$FIELD_INDEX], $condition=[$t13])
        CalciteEnumerableIndexScan(table=[[OpenSearch, opensearch-sql_test_index_weblogs]])
