{
  "calcite": {
    "logical": "LogicalSystemLimit(fetch=[10000], type=[QUERY_SIZE_LIMIT])\n  LogicalAggregate(group=[{}], min(account_number)=[MIN($0)], min(balance)=[MIN($1)], min(age)=[MIN($2)])\n    LogicalProject(account_number=[$0], balance=[$3], age=[$8])\n      CalciteLogicalIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]])\n",
    "physical": "EnumerableLimit(fetch=[10000])\n  EnumerableAggregate(group=[{}], min(account_number)=[MIN($0)], min(balance)=[MIN($3)], min(age)=[MIN($8)])\n    CalciteEnumerableIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]])\n"
  }
}