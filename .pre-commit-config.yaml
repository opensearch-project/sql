repos:
  - repo: local
    hooks:
      - id: spotless-format
        name: Spotless Format
        entry: bash -c './gradlew spotlessApply spotlessCheck && git add -u'
        language: system
        pass_filenames: false
