calcite:
  logical: |
    LogicalSystemLimit(fetch=[10000], type=[QUERY_SIZE_LIMIT])
      LogicalProject(take(employer, 0)=[$1], take(balance, -2)=[$2], gender=[$0])
        LogicalAggregate(group=[{0}], take(employer, 0)=[TAKE($1, $2)], take(balance, -2)=[TAKE($3, $4)])
          LogicalProject(gender=[$4], employer=[$8], $f2=[0], balance=[$7], $f4=[-2])
            CalciteLogicalIndexScan(table=[[OpenSearch, opensearch-sql_test_index_bank]])
  physical: |
    EnumerableLimit(fetch=[10000])
      EnumerableCalc(expr#0..2=[{inputs}], take(employer, 0)=[$t1], take(balance, -2)=[$t2], gender=[$t0])
        EnumerableAggregate(group=[{0}], take(employer, 0)=[TAKE($1, $2)], take(balance, -2)=[TAKE($3, $4)])
          EnumerableCalc(expr#0..2=[{inputs}], expr#3=[0], expr#4=[-2], proj#0..1=[{exprs}], $f2=[$t3], balance=[$t2], $f4=[$t4])
            CalciteEnumerableIndexScan(table=[[OpenSearch, opensearch-sql_test_index_bank]], PushDownContext=[[PROJECT->[gender, employer, balance]], OpenSearchRequestBuilder(sourceBuilder={"from":0,"timeout":"1m","_source":{"includes":["gender","employer","balance"],"excludes":[]}}, requestedTotalSize=2147483647, pageSize=null, startFrom=0)])
