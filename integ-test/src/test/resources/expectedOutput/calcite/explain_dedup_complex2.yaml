calcite:
  logical: |
    LogicalSystemLimit(fetch=[10000], type=[QUERY_SIZE_LIMIT])
      LogicalProject(account_number=[$0], gender=[$1], age=[$2], state=[$3])
        LogicalFilter(condition=[<=($4, SAFE_CAST(1:BIGINT))])
          LogicalProject(account_number=[$0], gender=[$1], age=[$2], state=[$3], _row_number_dedup_=[ROW_NUMBER() OVER (PARTITION BY $1, $3)])
            LogicalFilter(condition=[AND(IS NOT NULL($1), IS NOT NULL($3))])
              LogicalProject(account_number=[$0], gender=[$4], age=[$8], state=[$7])
                CalciteLogicalIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]])
  physical: |
    EnumerableLimit(fetch=[10000])
      EnumerableCalc(expr#0..4=[{inputs}], expr#5=[1:BIGINT], expr#6=[<=($t4, $t5)], proj#0..3=[{exprs}], $condition=[$t6])
        EnumerableWindow(window#0=[window(partition {1, 3} rows between UNBOUNDED PRECEDING and CURRENT ROW aggs [ROW_NUMBER()])])
          CalciteEnumerableIndexScan(table=[[OpenSearch, opensearch-sql_test_index_account]], PushDownContext=[[PROJECT->[account_number, gender, age, state], FILTER->AND(IS NOT NULL($1), IS NOT NULL($3))], OpenSearchRequestBuilder(sourceBuilder={"from":0,"timeout":"1m","query":{"bool":{"must":[{"exists":{"field":"gender","boost":1.0}},{"exists":{"field":"state","boost":1.0}}],"adjust_pure_negative":true,"boost":1.0}},"_source":{"includes":["account_number","gender","age","state"],"excludes":[]}}, requestedTotalSize=2147483647, pageSize=null, startFrom=0)])
