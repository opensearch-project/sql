calcite:
  logical: |
    LogicalSystemLimit(sort0=[$2], dir0=[ASC], fetch=[10000], type=[QUERY_SIZE_LIMIT])
      LogicalProject(username=[$0], skills_arr=[$1])
        LogicalUnnest
          LogicalProject(username=[$0], skills_arr=[$1])
            CalciteLogicalIndexScan(table=[[OpenSearch, mvexpand_edge_cases]])
  physical: |
    EnumerableLimit(fetch=[10000])
      EnumerableCalc(expr#0..2=[{inputs}], proj#0..1=[{exprs}])
        EnumerableUnnest
          EnumerableCalc(expr#0..2=[{inputs}], proj#0..1=[{exprs}])
            CalciteEnumerableIndexScan(table=[[OpenSearch, mvexpand_edge_cases]])
