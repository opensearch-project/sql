calcite:
  logical: |
    LogicalSystemLimit(fetch=[10000], type=[QUERY_SIZE_LIMIT])
      LogicalProject(sum=[$2], len=[$0], gender=[$1])
        LogicalAggregate(group=[{0, 1}], sum=[CHECKED_LONG_SUM($2)])
          LogicalProject(len=[CHAR_LENGTH($4)], gender=[$4], $f3=[+($7, 100)])
            CalciteLogicalIndexScan(table=[[OpenSearch, opensearch-sql_test_index_bank]])
  physical: |
    EnumerableLimit(fetch=[10000])
      EnumerableCalc(expr#0..2=[{inputs}], expr#3=[100:BIGINT], expr#4=[*($t2, $t3)], expr#5=[+($t1, $t4)], expr#6=[CHAR_LENGTH($t0)], sum=[$t5], len=[$t6], gender=[$t0])
        EnumerableAggregate(group=[{4}], sum_SUM=[CHECKED_LONG_SUM($7)], sum_COUNT=[COUNT($7)])
          CalciteEnumerableIndexScan(table=[[OpenSearch, opensearch-sql_test_index_bank]])
