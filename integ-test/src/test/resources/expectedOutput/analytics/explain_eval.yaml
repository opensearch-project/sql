calcite:
  logical: |
    LogicalSystemLimit(fetch=[10000], type=[QUERY_SIZE_LIMIT])
      LogicalProject(ip_addr=[$0], message=[$1], status=[$2], ts=[$3], error=[=($2, 500)])
        LogicalTableScan(table=[[opensearch, parquet_logs]])
